enum PAGES {
    HOME = "home",
    PROFILE = "profile",
    FEED = "feed"
}

enum DIALOG_POSITIONS {
    TOP = "top",
    CENTER = "center"
}

enum FEED_TABS {
    RECOMMENDED = "recommended",
    FOLLOWING = "following"
}

enum PROFILE_TABS {
    POSTS = "posts",
    FOLLOWINGS = "followings",
    FOLLOWERS = "followers"
}

export { PAGES,DIALOG_POSITIONS,FEED_TABS,PROFILE_TABS };